# Contracts/Dtos

## Purpose

DTOs (Data Transfer Objects) used at the Application boundary.

## Typical contents

- Request DTOs (Input Models) for use cases
- Response DTOs (Output Models) returned by use case handlers
- Lightweight projection DTOs for queries

## Out of scope

- Entities/Value Objects (they belong in Domain)
- Transport-specific types (HTTP response models)
